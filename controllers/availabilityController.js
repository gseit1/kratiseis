const Shop = require('../models/shop');
const Table = require('../models/table');
const reservation = require('../models/reservation')


const calculateShopAvailability = async (req,res)=>{
try {
    
} catch (error) {
    
}

};